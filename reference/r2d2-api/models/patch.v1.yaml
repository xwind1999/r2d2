title: Patch
type: object
x-tags:
  - auxiliar
x-examples: {}
properties:
  resource_uuid:
    type: string
    description: UUID of the resouce being patched
    format: uuid
  resource_golden_id:
    type: string
    description: Golden ID of the resource being patched
  resource_name:
    type: string
    enum:
      - room
      - roomprice
    description: Name of the resource being patched
  partial:
    type: object
    description: Resource payload containing only the data to be updated
required:
  - resource_name
  - partial
description: Pacth is a resource to map patch operations
